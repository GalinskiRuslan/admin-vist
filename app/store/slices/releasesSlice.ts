"use client";

import { createSlice } from "@reduxjs/toolkit";

export const releasesSlice = createSlice({
  name: "releases",
  initialState: {},
  reducers: {},
});

export const {} = releasesSlice.actions;

export default releasesSlice.reducer;
